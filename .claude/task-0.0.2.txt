你是番茄钟计时器项目的技术专家。

任务：迭代 0.0.2 — 按老板提供的参考设计图还原页面视觉。

先读 .claude/CLAUDE.md、docs/architecture.md、docs/iterations/0.0.2.md。

当前 src/index.html 已有基础实现（深蓝背景、标题、25:00显示、三按钮、状态机、归零高亮）。

需修改（参考图 evidence/owner/image.png 已由 Kimi 提取）：
1. 按钮样式改为圆角矩形、灰色细边框、深蓝色实心背景（非透明）
2. 暂停按钮非启用时文字灰色（明确灰色，非仅 opacity）
3. 底部增加恐龙奔跑 CSS 动画（参考图标注"这里更加一个小恐龙的奔跑动画"——用纯 CSS sprite/animation 实现一个可爱恐龙从左跑到右再回头的循环动画，像素风或简洁线条风）
4. 保持现有计时逻辑、状态机、归零高亮不变

技术约束：
- 纯 CSS 动画，无外部图片/SVG/JS 动画库
- 单文件自包含，不改架构
- 静态资源 URL 不带版本令牌（纯内联无外部资源）
- 先输出技术方案（如何实现恐龙动画、按钮改动），再实施
- 最终只能报告"自测完成，申请 Hermes 验收"，不得宣布交付
